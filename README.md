# telegram bot for remote control my christmas lights

how to set webhook for bot:

```bash
curl -F "url=https://<secure>.lambda-url.eu-central-1.on.aws" https://api.telegram.org/bot<YOURTOKEN>/setWebhook
```
